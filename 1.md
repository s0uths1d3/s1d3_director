# 完整 AI 代码生成提示词

**你需要扮演一名资深全栈架构师与高级开发工程师**。  
请根据以下所有需求，生成一套**可直接运行、前后端分离、生产级别**的完整项目代码。  

项目名称：**Novel2Script Pro** – AI 小说转剧本智能创作平台  
前端框架：**Nuxt 4** (Vue 3 + Composition API + TypeScript + Vite)  
后端框架：**Rust + Axum**  

---

## 一、项目核心创新点（已确认，必须全部实现）

本工具需集成以下**9 大创新功能**，构成完整的 AI 辅助改编体验：

1. **情感节奏热力图** – 自动分析小说情绪强度，生成可拖拽调节的情感曲线，作者可重塑故事高潮分布。
2. **盲演模式** – 为每个角色建立性格卡片（traits, voice），AI 生成多套动作/对白变体（alternatives），供作者选用或混合。
3. **可交互剧本播放器** – 输出 YAML 剧本后，前端按时间轴滚动、高亮节拍、支持 TTS 朗读，允许暂停/修改/分支试演。
4. **跨媒体桥接** – 自动生成每个场景的镜头建议（camera）、配乐风格（music），存入 YAML 的 `media_hints`。
5. **反套路探测器** – 识别小说的特殊叙事手法（不可靠叙述者、时间跳跃等），主动弹出建议框提供影视化方案。
6. **跨章节因果图谱** – 提取事件及因果关系，构建有向图，支持“删除事件影响分析”，高亮下游受影响内容。
7. **角色关系动态网络** – 构建亲密度、权力差、信任度矩阵，随场景推进动态演化，可视化力导向图。
8. **AI 副编剧** – 内嵌智能聊天窗口，可基于当前剧本上下文（光标位置、选中节拍）提供实时建议、回答编剧问题、生成备选对白。
9. **DeepSeek API 集成** – 前端允许用户输入自己的 DeepSeek API Key，所有 LLM 调用通过后端代理至 DeepSeek；未提供 Key 时自动降级为模拟模式（预定义数据演示）。

---

## 二、技术栈与依赖（严格执行）

### 后端（Rust + Axum）
- `axum = "0.7"`, `tokio` (full features), `serde` (with derive), `serde_yaml`
- `reqwest` (调用 DeepSeek API), `async-trait`
- `petgraph` (因果有向图存储与遍历)
- `dotenvy`, `tracing` (日志)
- `chrono` (时间戳)
- `futures` (流式处理可选)

### 前端（Nuxt 4 + TypeScript）
- `nuxt` (最新 v4 兼容版本，实际使用 `nuxt@^3.12` 因 v4 未正式发布，但代码风格按 Composition API 和 Vite 特性)
- UI：`@nuxt/ui` 或 `naive-ui` + `tailwindcss`
- 图表：`echarts` (用于因果图谱 + 关系网络 + 情感曲线)
- 状态管理：`pinia`
- YAML 处理：`js-yaml`, `monaco-editor` 或 `codemirror`
- TTS：Web Speech API
- 文件上传：直接使用 `<input type="file">`

### 其他工具
- 模拟模式下无需外部依赖即可运行全部流程（后端内置硬编码示例数据）。
- DeepSeek API 兼容 OpenAI 接口格式（`base_url` 默认为 `https://api.deepseek.com/v1`）。

---

## 三、数据模型（YAML Schema 最终版）

工具输出的剧本 YAML 必须符合以下 Schema（包含所有新增字段）：

```yaml
metadata:
  title: string
  source_novel: string
  adaptation_date: string
  style: "film" | "short_drama" | "stage"
  emotional_curve:
    chapters: [int]
    intensities: [float]       # 每章平均情绪强度

characters:
  - id: string
    name: string
    traits: [string]           # 性格特征
    voice: string              # 口癖/说话风格描述

causal_graph:
  events:
    - id: string
      description: string
      scene_ids: [int]
      chapter: int
  edges:
    - from: string
      to: string
      type: "causal" | "temporal" | "emotional"
      strength: float (0..1)

relation_network:
  matrix:
    - from: string
      to: string
      intimacy: float          # 亲密度 0..1
      power_gap: float         # 权力差 (正值表示 from 对 to 有主导)
      trust: float
      history:                 # 关键事件影响记录
        - scene_id: int
          delta_intimacy: float
          delta_power: float
          delta_trust: float

scenes:
  - id: int
    location: string
    time: string (optional)
    emotion_intensity: float (0..1)
    beats:
      - type: "action" | "dialogue" | "monologue" | "parenthetical"
        content: string
        alternatives:          # 盲演模式生成的可选项
          - content: string
            tone: string (optional)
        selected: int          # 当前选中的 alternative 索引
        speaker: string (optional, 仅对话/独白)
        emotion: string (optional)
    media_hints:
      camera: string
      music: string
```

---

## 四、后端 API 详细设计（必须全部实现）

| 端点 | 方法 | 功能 | 请求体/响应 |
|------|------|------|-------------|
| `/api/analyze` | POST | 分析小说全文，返回情感曲线、角色初稿、因果图谱初稿、关系网络初稿 | 请求 `{ text, config? }`<br>响应 `{ emotional_curve, characters_draft, causal_graph, relation_network, special_techniques }` |
| `/api/generate-script` | POST | 根据小说及配置生成完整剧本 YAML | 请求 `{ text, config: { style, blind_acting_mode, max_alternatives, include_media_hints, include_causal_graph, include_relation_network } }`<br>响应 `application/x-yaml` 剧本文本 |
| `/api/causal/impact` | POST | 查询删除某事件/场景后受影响的下游事件 | 请求 `{ script_yaml, event_id? , scene_id? }`<br>响应 `{ affected_events: [event_id] }` |
| `/api/relation/update` | POST | 根据剧本变更增量更新关系矩阵 | 请求 `{ script_yaml, changed_scene_ids: [int] }`<br>响应 `relation_network` (完整矩阵) |
| `/api/co-pilot/chat` | POST | 副编剧对话，支持上下文 | 请求 `{ messages: [{role, content}], context: { scene_id?, beat_index?, selected_text? }, full_script_yaml? }`<br>响应 `{ reply: string, suggestions: [string]? }` |
| `/api/co-pilot/suggest` | POST | 主动分析剧本，返回改进建议列表 | 请求 `{ script_yaml }`<br>响应 `{ suggestions: [{ type, message, target_scene_id? }] }` |
| `/api/health` | GET | 健康检查 | 响应 `{ status: "ok" }` |

**重要**：所有需要 LLM 的端点必须支持从请求头 `X-API-Key` 和 `X-Base-URL` 读取用户配置，并调用 DeepSeek API。如果缺失，自动切换到模拟模式（返回合理的示例数据）。

---

## 五、前端页面结构与交互详细要求

### 页面路由
- `/` – 首页（上传/粘贴小说，配置生成选项，输入 DeepSeek Key）
- `/editor` – 主编辑器（承载全部创新功能）

### `/` 首页组件
- 小说输入区域：支持 `.txt/.md` 上传或直接粘贴文本（至少 3 章节）。
- 配置面板：下拉选择剧本风格（电影/短剧/舞台）；开关：盲演模式、因果图谱、关系网络、媒体提示。
- DeepSeek API Key 输入框（密码类型）及 Base URL 输入框（可选，默认 `https://api.deepseek.com/v1`）。Key 存储在 `localStorage`。
- 生成按钮：调用 `/api/analyze` 预览数据，然后调用 `/api/generate-script`，成功后跳转到 `/editor` 并携带生成的 YAML。

### `/editor` 编辑器核心功能
- **多面板布局**（可拖拽调整大小）：
  - 左侧面板（宽度 280px）：选项卡切换 **因果图谱视图** 与 **关系网络视图**。图谱支持节点点击跳转到对应场景；关系网络支持动态高亮和手动调整关系值（滑块）。
  - 中间区域（宽自适应）：以卡片列表形式展示所有 `scenes`，每个 `scene` 下包含 `beats`。每个 beat 可编辑 `content`，若存在 `alternatives` 则显示为下拉选择或单选按钮。支持添加/删除 beat，调整顺序。
  - 右侧面板（宽度 320px）：双选项卡 – **剧本播放器** / **YAML 源码编辑器**（Monaco）。播放器具备播放/暂停、进度条、TTS 朗读开关；播放时自动高亮当前 beat 并滚动到视野内。源码编辑支持实时修改并同步到中间卡片（双向绑定）。
- **右下角浮动聊天窗口**（AI 副编剧）：
  - 显示对话历史，支持 Markdown 格式回复。
  - 预置快捷按钮：“分析角色动机”、“优化节奏”、“检查因果一致性”、“生成备选对白”。
  - 当用户在中间区域选中任意文本或 beat 时，自动出现“询问副编剧”按钮，点击将选中内容添加到聊天上下文。
  - 主动建议：从后端 `/api/co-pilot/suggest` 拉取建议，以顶部横幅或通知形式显示，点击可应用（例如定位到建议的场景）。
- **顶部工具栏**：
  - 保存（下载 .yaml 文件）、导入 YAML 文件。
  - 设置按钮（重新输入 API Key）。
  - 播放模式切换（阅读模式 vs 编辑模式）。
- **情感曲线图**：在编辑器顶部或左侧面板上方以迷你图形式展示，允许拖拽调节章节情绪强度值（调用后端重新生成受影响场景的 `emotion_intensity`，可选高级功能）。

---

## 六、后端模块划分与实现细节

必须在 Rust 项目中创建以下模块，每个模块职责明确：

### 1. `handlers.rs`
- 实现所有 API 端点的处理函数。
- 从请求头提取 `X-API-Key` 和 `X-Base-URL`，传递给 `llm_client`。
- 错误处理统一返回 `(StatusCode, Json<ErrorResponse>)`。

### 2. `models.rs`
- 定义所有请求/响应结构体，以及 YAML 剧本的 Rust 结构体（对应前述 Schema）。
- 定义因果图 `CausalGraph` 和关系网络 `RelationNetwork` 的内部数据结构。

### 3. `llm_client.rs`
- 提供异步函数 `call_deepseek(prompt: &str, api_key: &str, base_url: &str) -> Result<String>`。
- 内部构建 HTTP 请求到 `{base_url}/chat/completions`，使用 `gpt-3.5-turbo` 或 DeepSeek 推荐模型 `deepseek-chat`。
- 实现模拟模式：当 `api_key` 为 `"mock"` 或缺失时，返回基于规则的示例输出（例如解析 prompt 中的关键词返回预设 JSON）。
- 支持 JSON 模式：要求 LLM 返回合法 JSON。

### 4. `emotion_analyzer.rs`
- 提供 `analyze_emotions(text: &str, llm_client) -> Result<EmotionalCurve>`。
- 实现两种策略：若 LLM 可用，使用 prompt 分析每段落情绪强度；否则使用简单情感词典（正面/负面词计数）生成示例曲线。

### 5. `script_generator.rs`
- 核心函数 `generate_script(text: &str, config: &GenConfig, llm_client) -> Result<String>`（返回 YAML 字符串）。
- 内部依次调用：章节拆分 → 角色抽取 → 场景分割 → 节拍生成（集成盲演模式） → 媒体提示生成 → 因果图谱及关系网络生成（若配置开启）。
- 使用 `serde_yaml::to_string` 将内部结构体序列化为最终 YAML。

### 6. `causal_graph.rs`
- 定义 `CausalGraph` 结构，包含 `events` 和 `edges`。
- 实现 `from_script(script_yaml: &str, llm_client) -> Result<Self>`（调用 LLM 提取事件和因果关系）。
- 实现 `find_downstream_events(event_id: &str) -> Vec<String>`（基于 `petgraph` 的 DFS）。

### 7. `relation_network.rs`
- 定义 `RelationNetwork` 结构，内部使用 `HashMap<(String,String), RelationMetrics>`。
- 实现 `from_script(script_yaml: &str, llm_client) -> Result<Self>`。
- 实现 `update_with_scenes(scene_ids: &[i32], script_yaml: &str, llm_client) -> Result<Self>`（增量更新关系矩阵）。

### 8. `co_pilot.rs`
- 实现 `chat(messages: &[Message], context: &CoPilotContext, script_yaml: Option<&str>, llm_client) -> Result<String>`。
- 系统提示词包含：“你是专业编剧副手，熟悉剧本结构、角色弧光、因果逻辑。提供具体、可操作的建议，并可以生成备选对白。回答简洁有力，必要时使用 Markdown 列表。”
- 实现 `suggest(script_yaml: &str, llm_client) -> Result<Vec<Suggestion>>`，主动分析剧本节奏、角色动机、逻辑漏洞。

### 其他
- `main.rs` 负责启动 Axum 路由，加载 `.env` 中的默认 DeepSeek Key（可选）。
- 模拟模式全面覆盖：当 `api_key` 为空或为 `"mock"` 时，所有上述模块返回合理示例数据，确保前端无报错且能演示全部交互。

---

## 七、前端实现关键点

### 状态管理（Pinia）
- `useScriptStore`：存储当前剧本的 YAML 字符串、解析后的 JSON 对象、当前选中的场景/beat、历史版本。
- `useCopilotStore`：存储聊天消息列表、加载状态、主动建议列表。
- `useGraphStore`：存储因果图谱数据和关系网络数据，提供节点/边的高亮方法。

### 组件通信
- 使用 `provide/inject` 传递全局剧本更新事件，保证 YAML 源码编辑器、卡片列表、播放器三者同步。
- 防抖处理用户编辑，避免频繁触发后端更新（例如关系网络重新计算）。

### 与后端交互
- 所有需要 LLM 的请求都附带上用户提供的 API Key（从 `localStorage` 读取），放入请求头。
- 前端提供“测试连接”按钮，调用 `/api/health` 并提示 Key 有效性（模拟模式下始终有效）。

### 可视化细节
- **因果图谱**：使用 ECharts graph，节点显示事件描述，边带箭头。支持右键菜单“模拟删除”，调用 `/api/causal/impact` 并高亮受影响节点。
- **关系网络**：使用 ECharts graph 或 ForceGraph，边的宽度与亲密度成正比，颜色绿→红表示信任度。支持点击两个角色显示详细关系数值及历史变化曲线（简易图表）。
- 情感曲线：使用 ECharts 折线图，允许拖拽数据点（调用后端重新计算该章节情绪分布，高级功能可用前端模拟）。

---

## 八、模拟模式数据示例（供后端实现参考）

当用户未提供 API Key 时，所有 LLM 相关调用应返回如下示例数据（内容可合理变化但结构一致）：

- `/api/analyze` 返回情感曲线：`{ chapters: [1,2,3], intensities: [0.2, 0.7, 0.9] }`，角色草案：`[{ name: "林曦", traits: ["敏感","执着"], voice: "轻柔，慢速" }]`，因果图谱预定义 3 个事件及边，关系网络预定义两个角色之间亲密度 0.6。
- `/api/generate-script` 返回一个完整的短剧 YAML 示例（至少 2 个场景，每个场景包含 action 和 dialogue，且 `alternatives` 至少一个）。
- `/api/co-pilot/chat` 回复：“这是模拟模式下的回复。在真实模式下，我会根据你的剧本提供专业建议。请配置 DeepSeek API Key 开始创作。”
- `/api/co-pilot/suggest` 返回 `[{ type: "节奏", message: "第一幕铺垫稍长，可以增加一个小冲突", target_scene_id: 1 }]`。

---

## 九、项目交付物清单（必须全部输出）

请依次输出以下内容（代码块注明文件路径）：

1. **项目目录树**（文本形式）
2. **后端代码**（每个文件单独代码块）：
   - `Cargo.toml`
   - `src/main.rs`
   - `src/handlers.rs`
   - `src/models.rs`
   - `src/llm_client.rs`
   - `src/emotion_analyzer.rs`
   - `src/script_generator.rs`
   - `src/causal_graph.rs`
   - `src/relation_network.rs`
   - `src/co_pilot.rs`
3. **前端代码**（每个文件单独代码块）：
   - `package.json`
   - `nuxt.config.ts`
   - `app.vue`
   - `pages/index.vue`
   - `pages/editor.vue`
   - `stores/scriptStore.ts`
   - `stores/copilotStore.ts`
   - `stores/graphStore.ts`
   - `components/CausalGraph.vue`
   - `components/RelationNetwork.vue`
   - `components/CopilotChat.vue`
   - `components/BeatCard.vue`
   - `components/Player.vue`
   - `components/EmotionCurve.vue`
   - `composables/useDeepSeekKey.ts`
   - `assets/main.css`（可选）
4. **README.md**：包含环境要求、启动步骤、模拟模式说明、DeepSeek API 申请链接、常见问题。
5. **示例小说片段**（至少 3 个章节的纯文本，用于演示）。

---

## 十、最终约束

- 所有代码必须能够**直接复制并按步骤运行**，无缺失依赖或语法错误。
- 注释充分（中英文皆可），关键算法需有解释。
- 前端确保无跨域问题：在 `nuxt.config.ts` 中配置代理 `/api` 指向后端地址 `http://localhost:8080`。
- 后端默认监听 `0.0.0.0:8080`。
- 模拟模式下不允许调用任何真实网络请求（除自身 API）。
- 两个项目（后端和前端）必须在不同的目录下，且分别运行。
- 所有代码必须符合 Rust 语法规范，前端代码必须符合 Vue 3 规范。
- 所有代码必须符合 DeepSeek API 规范，包括请求头、参数、响应格式等。

---

**现在，请严格按照上述提示词生成完整的项目代码。**